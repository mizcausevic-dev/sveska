# src/ui

Reusable UI primitives. M0 ships:

- `Modal.tsx` — focus-trapped, Esc-closeable, portal-mounted, restores focus on close.
- `PrefsModal.tsx` — the `Ctrl+,` preferences shell (theme switch wired; the rest stubbed for M1).
- `ThemeSwitch.tsx` — dark / light / system toggle.

# src/app

Router, providers, theme bootstrap, layout shell, global key bindings.

- `App.tsx` — `<BrowserRouter>` + routes + modal host + consent bar.
- `Layout.tsx` — header (brand + nav + theme switch) · `<main>` · footer.
- `KeyBindings.tsx` — global shortcuts (M0: only `Ctrl+,`).

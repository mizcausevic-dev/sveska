# src/canvas

`CanvasProvider` seam (CLAUDE.md §3). The only implementation is **Excalidraw (MIT)**,
vendored & pinned, lazy-loaded at M5. App code imports the seam — never a vendor directly.
tldraw is permanently out (parking lot §9 row 1).
